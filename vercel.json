{
  "installCommand": "npm install",
  "buildCommand": "npm run generate:games && npm run copy:assets && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
